"""Inference-Time RTC evaluation sweep for LIBERO."""
# Imports are intentionally lazy: eval_libero_rtc pulls in LIBERO/robosuite
# which requires a GPU display (libGL). parse_eval_results_rtc is display-free.
